Create unit tests to the user flow following the pattern already did on auth flow.

Test every possible scenario (success and fail). Make sure that the coverage is in 100%.

Create mocks and fixtures in the right folders. Don't change the existing ones, only reutilize if necessary. Create new ones if necessary. You can also create new files in the mocks and fixtures folders if necessary.